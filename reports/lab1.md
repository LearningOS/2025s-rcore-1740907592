

1. 问题, 设计系统调用统计当前系统调用历史信息
2. 0 读取地址, 1写入地址, 2查询编号, trace也返回调用, 否则不关心
   1. 工程的数据条件操作
   2. 对应当前任务, 所以应该在当前任务下增加对应计数
      1. 每个人物对应枚举性质的数组, 维护一下, 或者使用结构体?
   3. syscall统计值, 对应模式匹配递增一下
   4. 获取项目管理器结构, 进行输出考察, 数据保存在结构里面
   5. 工程已知用的很多, 工程能力又变弱了,需要思考判断, 对内部取应用指针才要这样
   6. 所有权和可变借用问题, 复制结构体,修改的是副本, 所以直接修改内部元素就好了,可变借用
3. 模式匹配,读取数据, 应该用已知, 应该用学什么,然后测试
   1. 类的思考, 不要暴露外界数据, 直接提供 函数接口更好

